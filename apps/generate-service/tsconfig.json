{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".", // ✅ Allow all files under this folder
    "baseUrl": "./"
  },
  "include": ["src", "prisma/seed.ts"], // ✅ Still only compiles what's needed
  "exclude": ["node_modules", "dist"]
}
